<a href="{{ $location }}"  {{ $attributes }}>
<button type="button" class="btn btn-icon-text-primary {{ $class }}" id="{{ $id }}"
    onclick="{{ $onClick }}" data-bs-toggle="{{ $toggle }}" data-bs-target="{{ $target }}">
    <i class="{{ $icon }}"></i>
    {{ $text }}

</button>
</a>
