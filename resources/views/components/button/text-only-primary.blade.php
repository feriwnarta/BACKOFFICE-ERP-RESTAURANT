<button type="{{ $type }}" class="btn btn-text-only-primary {{ $class }}" id="{{ $id }}"
    onclick="{{ $onClick }}" data-bs-toggle="{{ $toggle }}" data-bs-target="{{ $target }}">
    {{ $text }} </button>
