<div class="{{ $class }}">
  <label for="{{ $id }}" class="form-label">{{ $label }}</label>
  <textarea class="form-control textarea" id="{{ $id }}" rows="5" placeholder="{{ $placeHolder}}"></textarea>
</div>
