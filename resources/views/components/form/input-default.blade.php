<div class="container-input-default">
    <label for="{{ $id }}" class="form-label input-label">{{ $label}}</label>
    <input type="name" name="{{ $name }}" class="form-control input-default {{ $class }}" id="{{ $id }}" placeholder="{{ $placeHolder }}">
</div>
